# 策划实用小工具在线版测试计划

## 1. 测试目标

测试证明的不是“新系统能打开”，而是以下契约同时成立：

1. 已有主图协同版账号、密码哈希、会话 Cookie 可直接使用。
2. 在线版业务表和迁移不会污染共享认证表、主图业务表或其 Alembic revision 链。
3. 除登录页外，页面收集和数据分析的 UI、API 与业务结果和旧版一致。
4. macOS/Windows Agent 能在不暴露本地端口的条件下完成反向取任务、采集、暂停/继续、登录和定时。
5. 本地存储与腾讯云 COS 遵守同一 `BlobStorage` 契约，断网/重试不丢图、不重复。
6. 中心服务、Agent、数据库或 COS 任一重启/短时故障后均可恢复。

## 2. 测试层级与门禁

| 层级 | 内容 | 合并/交付门禁 |
|---|---|---|
| 静态检查 | 格式、类型、依赖、密钥扫描、迁移危险操作扫描 | 必须通过 |
| 单元测试 | 密码/会话、指标、状态机、路径、摘要、存储适配器 | 必须通过 |
| 集成测试 | FastAPI + PostgreSQL + Local/COS mock + Agent fake | 必须通过 |
| 契约测试 | 旧 API 请求/响应快照、新旧分析结果对比 | 必须通过 |
| E2E | 登录、上传、分析、采集、图库、调度、退出 | 必须通过 |
| 视觉回归 | 固定 Chromium/视口下的像素截图 | 非登录页不得有主动改版 |
| 平台验收 | macOS 与 Windows 安装、升级、自启、卸载 | 两平台必须通过 |
| 生产演练 | 真实 PostgreSQL、COS 私有桶、备份恢复、互斥端口 | 上线前必须通过 |

## 3. 固定环境矩阵

### 3.1 服务端

| 维度 | 最小覆盖 |
|---|---|
| Python | 3.11、最终打包版本 |
| 数据库 | PostgreSQL 与生产同主版本；SQLite 仅可用于快速单元测试 |
| 存储 | `LocalBlobStorage`、COS 模拟服务/SDK stub、腾讯云测试私有桶 |
| Cookie | 本地 HTTP `Secure=false`、生产 HTTPS `Secure=true` |
| 浏览器 | 固定 Chromium；补充当前 Edge 和 Chrome 冒烟 |
| 中心 OS | 实际受限中心主机环境 |

### 3.2 客户端

| 平台 | 覆盖 |
|---|---|
| macOS | 当前支持的 Intel/Apple Silicon 范围、Keychain、launchd、浏览器路径 |
| Windows | Windows 10/11 x64、Credential Manager、任务计划程序、中文用户名路径 |
| 网络 | 正常、断网、慢网、上传中断、代理/防火墙、中心重启 |

## 4. 测试基线与夹具

- 从旧程序保存 2025、2026 两个真实 Excel 的只读副本及 SHA-256。
- 保存旧 `/api/analysis/*` 所有面板在固定参数集下的完整 JSON；比较时忽略明确声明的动态字段，其余字段、数组顺序、数值均比较。
- 保存现有 27 张截图的相对品牌/日期/文件名、字节数、尺寸、SHA-256 和总字节数。
- 固定一组小 PNG、11–12 MB 长图、30 MB 边界图、超限图、伪装扩展名、损坏图和超大像素炸弹样本。
- 在隔离 PostgreSQL 中从参照迁移链建立 `users/user_sessions`，准备 active/inactive、正确/错误密码、过期/有效 session。
- UI 黄金图覆盖：集合页、截图页、分析各面板、弹窗、进度浮层、大图灯箱；浅/深色，默认/自定义主题，固定 DPR、字体、视口、浏览器版本和测试时钟。
- Agent fake 支持成功、超时、重复消息、租约过期、乱序进度、断网、验证码和平台能力差异。

真实数据含业务信息，不提交公共仓库；CI 使用脱敏等价夹具，生产前在授权环境运行真实基线。

## 5. 认证与会话测试

| ID | 场景 | 预期 |
|---|---|---|
| AUTH-01 | 用参照版已建账号和正确密码登录 | 成功，不新建/复制用户 |
| AUTH-02 | 读取参照算法生成的 PBKDF2 哈希 | 600000 轮兼容验证 |
| AUTH-03 | 用户不存在、密码错、inactive | 均 401 且文案不泄露差异 |
| AUTH-04 | 登录后查数据库 | 只存 64 字符 token hash，不存明文 |
| AUTH-05 | Cookie 属性 | 名为 `main_image_collab_session`，HttpOnly、Path=/、SameSite=Lax；HTTPS 有 Secure |
| AUTH-06 | 参照版签发的同域有效 Cookie | 在线版 `/api/auth/status` 和 `/me` 可识别 |
| AUTH-07 | 过期、篡改、空 Cookie | 401/status 未认证 |
| AUTH-08 | inactive 用户持有旧有效 session | 仍拒绝 |
| AUTH-09 | 会话访问 | 更新 `last_seen_at`，过期时间不意外延长 |
| AUTH-10 | logout | 删除共享会话并清 Cookie；行为与参照版一致 |
| AUTH-11 | 未登录访问页面和全部业务 API | 显示登录页/API 401，不泄露数据 |
| AUTH-12 | 两个不同账号 | 分析、截图、店铺、任务严格按 user_id 隔离 |
| AUTH-13 | CSRF/Origin | 跨站变更请求被拒绝，同源正常 |
| AUTH-14 | 登录爆破 | 达到阈值后限速，不影响已登录读取 |

## 6. 数据库与 Alembic 测试

| ID | 场景 | 预期 |
|---|---|---|
| DB-01 | 同库已有参照版完整 schema 时升级在线版 | 成功，只新增 `practical_*` 和 `practical_alembic_version` |
| DB-02 | 查询两条 version 链 | `alembic_version` 不变；在线版 revision 写入独立表 |
| DB-03 | 审查 upgrade/downgrade SQL | 不含 create/alter/drop `users`、`user_sessions` 或主图业务表 |
| DB-04 | 在线版 upgrade → downgrade → upgrade | 共享表数据、索引、约束、行数与摘要不变 |
| DB-05 | 空库未先部署共享认证 schema | 在线版迁移明确失败并提示前置条件，不偷偷创建账号表 |
| DB-06 | `users.id` 外键 | 有效用户可写，非法用户被 PostgreSQL 拒绝 |
| DB-07 | 业务唯一约束 | `capture_id`、用户年度、用户日期、修正项正确去重 |
| DB-08 | 生产配置 | 不调用 `create_all`；必须显式迁移到 head |
| DB-09 | 并发年度重传/删除 | 事务完整，无半套日数据或悬空当前引用 |
| DB-10 | PostgreSQL 时区 | 时间均为 timezone-aware UTC，展示按配置时区 |

迁移门禁脚本应记录执行前后所有非 `practical_*` 表的 schema fingerprint 和关键表行数；任一变化即失败。

## 7. 旧 API 契约测试

### 7.1 页面收集

| ID | 接口/场景 | 核心断言 |
|---|---|---|
| SC-01 | `GET /list` | `brands/total` 结构、品牌升序、日期/图片倒序、count 正确 |
| SC-02 | `/file`、`/thumb` | 原图可读；缩略图 JPEG/宽不超 480；无权/不存在正确失败 |
| SC-03 | `POST /delete` | paths/id 批量删除，返回 `deleted`；跨用户 id 无效 |
| SC-04 | `/shops` 增删 | 原字段和重复名称错误保持；URL 校验不破坏合法旧数据 |
| SC-05 | 三种 `/capture` | 全部、单店、任意 URL 创建正确 job；已有活动任务返回旧式 error |
| SC-06 | `/capture/status` | running/paused 与 Agent/job 状态一致 |
| SC-07 | pause/resume | Agent 在线生效，重复调用幂等；离线返回可展示 error |
| SC-08 | `/progress` | current/total/shop 类型和值与上报一致，乱序上报不倒退 |
| SC-09 | `/login*` | 指令由 Agent 本地执行，只返回状态，不上传淘宝秘密 |
| SC-10 | `/scheduler/*` | 原子保存启用、状态、安装、卸载、星期/时分字段与旧 UI 兼容 |
| SC-11 | `/open-folder` | 仅向所属在线 Agent 下发；服务端不尝试打开中心机目录 |
| SC-12 | Agent 离线 | 不产生假成功；页面能按现有 error 分支提示 |

### 7.2 数据分析

对以下接口逐一保存新旧响应快照：`upload`、`status`、`preview`、`files`、`file`、`metrics`、`panel/overview`、`panel/newold`、`panel/anomaly`、`panel/anomaly/fix`、`panel/anomaly/fixes`、`panel/blocks`、`panel/blocks_yoy`、`panel/calendar`、`panel/overall_all`。

参数矩阵：

- 年份：2025、2026、缺省、无数据年份。
- 维度：MTD 1/12、QTD 1/4、HTD 1/2、YTD、自定义单年/跨年。
- 当前期和自定义对比期；无上一年数据。
- 活动筛选：普通、常规分段、不存在。
- 板块指标：点击率/支付转化，新客/老客/整体，include 缺省/空/部分，min_days 边界。
- 异常修正：访客数/点击人数，正/负/零、小数、覆盖、删除、非法字段。

数值比较规则：整数精确相等；旧接口已 round 的浮点精确比较序列化值，未 round 的计算采用绝对/相对误差 `1e-9`；日期、键集合、数组顺序、`None` 位置必须一致。

| ID | 场景 | 预期 |
|---|---|---|
| AN-01 | 上传两个真实 Excel | 年份、sheet、字段映射、异常数、预览与旧版一致 |
| AN-02 | 重传同年度修正版 | 整年度替换，删去源中已不存在日期，不残留旧行 |
| AN-03 | 删除年度 | 源文件引用、缓存、daily、fix 均按旧语义清理 |
| AN-04 | 全面板快照 | JSON 字段、顺序、范围、数值与旧基线一致 |
| AN-05 | 两账号相同年份 | 数据互不覆盖 |
| AN-06 | 解析失败/损坏/超限文件 | 旧有效年度仍可用，无临时 Blob 泄漏 |
| AN-07 | 并发读取与上传 | 读取看到旧完整版本或新完整版本，不见半成品 |

## 8. Agent 协议与状态机测试

| ID | 场景 | 预期 |
|---|---|---|
| AG-01 | 单次配对码 | 仅能兑换一次，过期/错用户拒绝 |
| AG-02 | token 存储 | 服务端仅 hash；macOS Keychain/Windows Credential Manager 无明文配置文件 |
| AG-03 | 反向长轮询 | 无任务 204；无需监听本地端口 |
| AG-04 | 原子 start | 两 Agent 同抢一 job 只有一个成功 |
| AG-05 | 租约续期/过期 | 活跃任务续期；Agent 崩溃后可恢复，不永久 running |
| AG-06 | 重复 start/progress/complete | 幂等，无重复截图/重复计数 |
| AG-07 | 乱序进度 | 完成数不回退，完成后进度不能改写 |
| AG-08 | pause/resume/stop | 采集安全点响应，暂停不丢 outbox，停止状态明确 |
| AG-09 | 淘宝登录 | 只上报状态；抓包、日志、数据库无 Cookie/storage state |
| AG-10 | 验证码 | 状态可见，任务不误报成功，人工完成后可继续 |
| AG-11 | 中心断网 | 已抓图片落 outbox；恢复后自动上传 |
| AG-12 | Agent 重启 | outbox 和活动任务恢复/重新协调，无重复 |
| AG-13 | 配置版本 | 店铺/调度更新最终一致，旧版本请求可重试 |
| AG-14 | token 撤销 | 下一次轮询即拒绝，已签发短时上传按策略过期 |
| AG-15 | 版本过低 | 服务端给出升级提示，不下发不兼容任务 |

## 9. BlobStorage 与 COS 测试

### 9.1 适配器统一契约

同一组测试必须参数化运行在 `LocalBlobStorage` 和 `TencentCosBlobStorage`（SDK stub/测试桶）上：

| ID | 场景 | 预期 |
|---|---|---|
| BS-01 | put/head/read/delete | bytes、size、MIME、metadata 一致；删除幂等 |
| BS-02 | storage key | 拒绝绝对路径、`..`、编码穿越和跨用户前缀 |
| BS-03 | 同 key 重试 | 同摘要幂等；不同内容冲突，不静默覆盖 |
| BS-04 | 部分写入/进程崩溃 | Local 不暴露半文件；COS 未完成对象不激活元数据 |
| BS-05 | provider 切换 | 历史记录仍按其 provider 读取，不因默认值变化失联 |

### 9.2 预签名上传与完成确认

| ID | 场景 | 预期 |
|---|---|---|
| COS-01 | 合法任务申请上传 | 返回短时、限定 key/方法/头的签名，不含永久密钥 |
| COS-02 | 非所属/撤销 Agent 申请 | 403；不创建对象或 session |
| COS-03 | 过期签名、改 key、改 method、漏签名头 | COS 拒绝 |
| COS-04 | 正常 PUT + confirm | HEAD size/type/metadata 和流式 SHA-256 校验通过后才建截图记录 |
| COS-05 | ETag 与 SHA-256 不同 | 系统不把 ETag 当摘要，仍正确验证 |
| COS-06 | size/sha256/MIME 不符 | confirm 失败，对象隔离/清理，不激活截图 |
| COS-07 | 同 capture_id 同 sha 重复申请/确认 | 返回同一截图，数据库仅一行 |
| COS-08 | 同 capture_id 不同 sha | 409 并保留原成功对象 |
| COS-09 | 上传中断后重试 | 同一 outbox 项最终一次成功，无重复对象记录 |
| COS-10 | 11MB、30MB、超限 | 前两者按配置成功，超限在签名申请前拒绝 |
| COS-11 | 私有下载 | 无签名不可读；鉴权 API 仅给所属用户短时 GET |
| COS-12 | 下载签名过期 | 过期拒绝；刷新页面可获取新签名 |
| COS-13 | CORS 正式 origin | 允许所需 GET/HEAD（及配置的 PUT）；恶意 origin 预检失败 |
| COS-14 | 密钥泄漏扫描 | 前端、Agent 包、日志、错误、URL 中无 SecretId/SecretKey |
| COS-15 | COS 未配置 | 明确告警并使用 Local |
| COS-16 | COS 已配置但鉴权失败 | 生产默认启动失败；只有显式开关才告警降级 |
| COS-17 | 桶限流/5xx | 指数退避、可观测、outbox 不删除 |
| COS-18 | 缩略图 | 生成一次后复用，宽度/格式/鉴权/删除联动正确 |

腾讯云测试桶必须独立、私有、设生命周期自动清理测试前缀；每次测试使用唯一前缀，结束时核对并清理。

## 10. UI 与端到端测试

### 10.1 视觉回归

对旧版和在线版使用同一 Chromium、字体、DPR、视口、语言、时区、数据和冻结时钟截图。比较：

- 集合页：仅允许登录页和两个排除入口的已确认差异。
- 截图页：空态、品牌列表、日期展开、选择/删除、灯箱各缩放、店铺、立即截图、登录、定时、进度。
- 分析页：数据管理、总览、新老客、异常、板块、同比、日历、全体指标及所有下拉/弹窗/tooltip。
- 浅色/深色、默认主题/自定义主题和背景。

验收使用像素差异图并人工审查。非动态区域目标为完全一致；抗锯齿等平台噪声需建立小阈值和遮罩，但不得用大面积遮罩掩盖布局差异。

### 10.2 核心 E2E

| ID | 流程 | 预期 |
|---|---|---|
| E2E-01 | 未登录 → 登录 → 集合页 | 只新增登录门禁，成功后进入原 UI |
| E2E-02 | 上传 2025/2026 → 切换各面板 → 修正 → 删除 | 页面行为和旧版一致 |
| E2E-03 | 在线 Agent → 全部店铺采集 → 进度 → COS 图库 | 图片不重不丢，可看原图/缩略图 |
| E2E-04 | 单店与任意 URL | 名称、品牌、时间和结果一致 |
| E2E-05 | 登录淘宝/验证码 | 本地窗口完成后网页状态更新 |
| E2E-06 | 暂停 → 重启中心 → 恢复 → 继续 | 状态可恢复，最终上传完整 |
| E2E-07 | 断网采集两张 → 恢复 | outbox 自动清空到已确认，两张各一条 |
| E2E-08 | 定时任务 → 重启电脑 → 到时执行 | macOS/Windows 均触发并上传 |
| E2E-09 | 打开图片文件夹 | 打开 Agent 本地目录，不打开中心目录 |
| E2E-10 | 退出 | 网站注销/Agent 停止符合产品语义，中心服务仍运行 |

## 11. 安全、性能与恢复测试

### 11.1 安全

- 未授权/越权枚举 screenshot id、storage key、year、job id、agent id。
- 文件名、品牌名、店铺名和 Excel 单元格的 XSS/公式注入/日志注入。
- 上传魔数不符、压缩炸弹、像素炸弹、路径穿越、符号链接逃逸。
- favicon SSRF：127.0.0.1、IPv6 回环、RFC1918、link-local、云元数据、DNS rebinding、重定向到内网。
- 预签名 URL 重放、超时、跨 key、签名头篡改；日志脱敏。
- 依赖漏洞和安装包密钥/淘宝状态/测试凭据扫描。

### 11.2 性能目标

基线值在首轮实测后冻结，至少验证：

- 730 天分析数据与真实 Excel 下所有面板响应时间不劣于旧版的可感知体验。
- 27 张现有截图首屏使用缩略图，不下载全部原图；列表不会因单张 11 MB 长图阻塞。
- 12 店铺一轮约 96 MB 上传时，中心 API 仍可登录、查看分析和查询进度。
- 1000/10000 张截图下分页/分组策略不会导致中心内存失控。
- Agent 心跳和长轮询在预期客户端数下不会造成数据库热循环。

### 11.3 故障与恢复

| ID | 故障点 | 预期 |
|---|---|---|
| REC-01 | 上传前 Agent 崩溃 | outbox 文件保留，重启后续传 |
| REC-02 | COS 成功、confirm 前崩溃 | 重试 confirm 幂等，或后台回收孤儿对象 |
| REC-03 | confirm 事务中中心崩溃 | 不出现半激活截图，重启可重试 |
| REC-04 | PostgreSQL 暂停/重启 | 请求明确失败/恢复，任务状态不丢 |
| REC-05 | 中心重启时 job running | 租约恢复策略生效，不永久卡住 |
| REC-06 | COS 临时 403/429/5xx | 不删 outbox，退避并告警 |
| REC-07 | 备份恢复到空环境 | 账号、分析、截图元数据和 Blob 全部对账 |
| REC-08 | 回滚旧程序 | 原数据快照仍在，切换不需删除新库或源数据 |

恢复后对账：截图行数、按品牌/日期计数、总字节、逐对象 SHA-256；分析年份、daily 行数、fix 数量和全接口快照。

## 12. 端口、安装与运维验收

| ID | 场景 | 预期 |
|---|---|---|
| OPS-01 | 无占用启动 | 默认监听 `18180` |
| OPS-02 | 主图协同版已占 `18180` | 本程序拒绝启动并明确提示，不结束对方进程 |
| OPS-03 | 本程序已运行再启动 | 单实例提示，不生成第二个中心进程 |
| OPS-04 | 用测试配置覆盖端口 | 可在隔离环境启动，不改变生产模板默认值 |
| OPS-05 | macOS/Windows 首次安装 | Agent 依赖、浏览器、凭据目录和自启正确 |
| OPS-06 | 覆盖升级 | 淘宝状态、设备 token、outbox、配置不丢 |
| OPS-07 | 卸载 | 明确区分程序文件、可恢复数据、淘宝状态；不误删用户目录 |
| OPS-08 | 日志轮转/磁盘不足 | 有告警，不写坏数据库/截图；恢复空间后可继续 |

## 13. 发布判定

只有同时满足以下条件才能标记可交付：

- 所有 P0/P1 测试通过，无已知数据丢失、越权、密钥泄漏或共享库破坏问题。
- 旧 API 契约和真实 Excel 面板对比全部通过。
- 非登录页视觉差异均有逐项解释并经用户确认；不得以“在线版”为由接受主动重设计。
- macOS、Windows 各完成一次真实淘宝登录和至少一轮真实采集。
- Local 与 COS 两种存储契约通过；COS 私有下载、预签名上传、SHA-256 和幂等通过。
- 共享 PostgreSQL 的 `alembic_version`、`users`、`user_sessions` 和主图业务 schema 经前后 fingerprint 对账无意外变化。
- 完成一次 PostgreSQL + Blob 空环境恢复演练和一次 `18180` 互斥启动演练。
- 交付验收报告包含版本、环境、测试证据、失败项、已知限制、备份位置和回滚步骤。
