# Android 连接诊断工具计划

## 目标

在 APK 登录前加入一个原生 Android「连接诊断」工具，用于定位 HarmonyOS/卓易通以及普通 Android 设备访问中心服务失败的具体链路，并输出可复制的脱敏诊断报告。

## 阶段

- [x] 1. 检查现有 Android 工程、入口和构建基线
- [x] 2. 实现独立的连接诊断引擎
- [x] 3. 实现原生诊断页面与登录前入口
- [x] 4. 补充静态检查与构建验证
- [x] 5. 在 Android 模拟器上实机检查页面、状态栏和交互
- [x] 6. 生成可交付 APK 并记录诊断工具使用说明
- [x] 7. 排查截图原图加载链路并增加移动端高清预览
- [x] 8. 编译、安装并验证图片加载改版的 Android 包
- [x] 9. 将完整分析工具拆成原生分析工作区
- [x] 10. 修复原生数据请求压缩/连接复用问题并验证加载链路
- [x] 11. 打包并校验中心服务在线更新包
- [x] 12. 重做原生功能页视觉系统与自适应信息架构
- [x] 13. 在手机、平板、横屏和大字体下逐页截图验收
- [x] 14. 生成新版 Android 测试 APK 并交付页面截图与变更说明
- [x] 15. 修复中心在线更新后重启时的 Windows 终端窗口
- [x] 16. 修复手机高清截图预览的有损压缩模糊问题
- [x] 17. 保留页面采集返回图片前的筛选、列表和滚动状态
- [x] 18. 引入官方 Impeccable 与 GSAP 动画技能，并建立项目级 Premium UI 规范适配器
- [x] 19. 按设计系统全量重构 Android 原生页面并完成逐设备截图验收
- [x] 20. 清理登录与功能页的宣传型、实现型和重复说明文案
- [x] 21. 使用本地测试账号逐页截图验收全部原生页面

## 设计约束

- 诊断页面不使用 WebView；网络探测在后台线程执行，界面只显示结果。
- 不读取、不保存、不发送账号和密码；仅检测用户填写的中心地址。
- 所有内容适配状态栏、安全区域和不同宽度，按钮保持可点击但视觉高度紧凑。
- 诊断结果使用“通过 / 注意 / 失败 / 检测中”四种明确状态，避免只显示笼统的 SocketException。
- 生产默认只允许 HTTPS；诊断会把 HTTP 地址标记为安全风险，而不是静默放行。

## 当前决策

- 诊断入口放在登录页，登录前即可使用。
- 检测链路按设备环境 → 网络 → 地址解析 → DNS → TCP → TLS → HTTPS 响应的顺序展示。
- 对 HarmonyOS/卓易通常见问题给出针对性建议，包括 DNS、IPv6、证书/SNI、代理/VPN、网络权限与后台联网限制。
- 真实设备报告中 HarmonyOS 仅作为环境信息标记为“通过”；TLS 失败按 IPv4/IPv6 分组完整展示，避免误导排查方向。

## 图片加载与原生化范围

- 原图查看不再先后下载两次；手机端优先请求服务端缓存的高清预览，老版本中心没有该接口时回退原图单次下载。
- 预览图按最长边限制在 640–4096 像素范围内；手机端优先使用无损 WebP，兼容环境再回退到高质量 JPEG，避免高长截图和中文小字被有损压缩。
- 图片查看页显示加载状态、失败原因和重试入口；列表缩略图、原生查看器和网页端原图能力彼此独立。
- Android 当前业务代码已不再保留 WebView 页面入口；完整分析能力由原生分析工作区承载，板块矩阵在手机端采用纵向可读列表展示。

## 本轮全原生与性能计划

- 原生分析工作区覆盖总览、板块深度、新老客、活动日常、异常检测、修正记录和数据管理。
- 原生 API 请求声明 `Accept-Encoding: gzip` 并透明解压中心返回的 GZip JSON，减少分析和列表数据的传输体积；保持 Cookie、HTTPS 和错误映射不变。
- 完整中心在线更新包使用项目既有 `practical-tools-online-update-v1` 清单校验流程，版本以 `app/__version__.py` 为准，不携带 `.env`、data、runtime 或密钥。

## 新一轮 UI 重设计原则

- 依据 Android Window Size Class：紧凑宽度使用底部/页面内导航，中等宽度使用导航栏或双栏，展开宽度使用持久侧栏或列表-详情双窗格；不把手机单栏界面直接拉伸到平板。
- 页面采用统一的浅色数据工作台：深墨蓝文字、冷灰背景、蓝紫主行动色，成功/警告/失败使用独立语义色；减少厚重描边和过大的实心按钮。
- 交互控件保持约 40–48dp 可点击高度，但视觉内容采用更紧凑的内边距；按钮之间保留 8–12dp 呼吸空间，不再把一组操作堆成连续满宽条。
- 图片收集改为“状态摘要 → 主要操作 → 筛选芯片 → 最近内容”的层次；分析改为“筛选工具栏 → 指标卡 → 分组内容”，详情图片与数据卡保持独立原生页面。
- 所有页面继续使用 `WindowInsets` 处理状态栏、导航栏、刘海和手势区；字体、卡片、按钮、错误状态和加载状态统一由原生组件样式生成。
- 全量 UI 重构必须通过三层检查：Impeccable 的原生适配/可用性原则、项目级 Premium UI 硬约束、GSAP core/timeline/performance 的动效原则，并将 GSAP 动效语义映射为 Android 原生动画 API，不把 GSAP 运行时引入 APK。
- 已将 GSAP 的 transform/opacity、自然缓动、可取消和 reduced-motion 原则映射为 Android 原生触控反馈与页面淡入；后续页面级动效继续沿用这套入口。
- 工具名称统一使用“策划实用小工具”；登录页不再使用“把工作台带到手机上”这类宣传句，功能页只保留影响操作、状态、错误、安全或恢复的必要文字。
- 逐页验收不能只覆盖登录页；必须在本地测试中心登录后，对工作台、页面收集、图片查看、分析、诊断、设置、店铺管理和定时任务分别截图，并覆盖窄屏、长屏、平板、横屏和大字体场景。
- 间距采用响应式公共策略：普通手机默认 8dp，平板或大字体使用 16dp，短屏/窄屏优先收紧；所有值仍由 `dp()` 归一到 8dp 网格，避免显式间距与公共间距叠加造成模块过松。
- Android 0.4.4 使用统一的蓝紫 P 产品图标，并在分析工作区将周期筛选置于上方、业务模块置于底部 Tab；总览新增本期/对比期数据和原生 Canvas 趋势图。
- [x] 按实机反馈拆分模块级/模块内间距，移除重复 margin 并重新构建 0.4.4 APK
