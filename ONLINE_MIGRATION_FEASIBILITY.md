# 策划实用小工具在线化可行性与实施规划

## 1. 结论先行

**整体可行，可行性高。**

本次实际迁移范围是：

1. 页面收集
2. 首页数据分析
3. 集合外壳中与上述两个功能相关的导航、主题、背景、收藏、头像、状态和退出行为。

“首页数据分析”可以做成纯在线功能；“页面收集”不能只靠普通网页完成，必须保留一个运行在用户电脑上的轻量采集端。推荐的最终形态是：

```text
在线网页（原 UI 原样复用）
        │
        ├── 同源 HTTPS API ──────> FastAPI 中心服务
        │                              ├─ PostgreSQL
        │                              ├─ 截图/Excel 存储
        │                              └─ 登录会话
        │
        └── 页面收集指令─────> 后端任务队列
                                         ↑ 轮询/长连
                                  本地采集 Agent
                                  ├─ Patchright
                                  ├─ 本地淘宝登录态
                                  ├─ OCR/裁剪/验证码检测
                                  └─ 截图后断点上传
```

这不需要多用户项目锁、行编辑锁或协同版本冲突机制。只需要普通数据库事务、重复上传去重和任务幂等，这些属于基本可靠性，不是协同锁。

## 2. 代码盘点与事实依据

### 2.1 待迁移程序

`/Users/hanyang/策划实用小工具` 当前是单机 FastAPI 服务，同时托管多个静态 HTML/JS 子应用：

- `/banword`：违禁词排查，本次排除。
- `/screenshot`：页面收集，本次迁移。
- `/analysis`：首页数据分析，本次迁移。
- `/main-image`：主图一条龙服务，本次排除。
- `/`：集合外壳，通过 iframe 加载子应用。

前端没有大型框架绑定，并且与后端使用同源相对 API 路径。这对“原样搬迁”非常有利：HTML、CSS、DOM 结构、图标、动画、对话框和大部分 JavaScript 可直接复用。

### 2.2 参照程序可复用的基建

`/Users/hanyang/主图一条龙服务协同版/协同版` 已验证了如下能力：

- FastAPI 中心服务与静态网页托管。
- SQLAlchemy + Alembic + PostgreSQL。
- 用户、密码哈希、HttpOnly Cookie 会话、登录/退出/状态 API。
- 安全的存储 key、文件校验、缩略图缓存、导出与备份目录。
- Windows 中心机部署、数据迁移、启动恢复与在线更新的工程经验。

建议复用这些“基础能力的设计”，但不整包复制协同版业务代码。本项目不需要角色、项目成员、审核分工、并发锁、修改轮次、COS 协同会话等复杂模块。

### 2.3 现有数据规模

实地盘点到：

- 截图 27 张，共约 217 MB，平均约 8.04 MB/张，最大约 11.26 MB。
- 分析数据已有 2025、2026 两年，`analysis_daily` 共 730 行。
- 分析缓存约 2.3 MB，两个源 Excel 合计不到 1 MB。

当前数据规模不大，一次性迁移很轻量。但截图长期增长很快：按现有平均估算，12 家店铺每轮约产生 96 MB。上线时必须一并定义保留期、备份与容量告警。

## 3. 功能可行性矩阵

| 功能 | 现状 | 在线化方法 | UI 原样保持 | 可行性 |
|---|---|---|---|---|
| 集合页、标签、iframe 切换 | 静态 HTML/JS | 直接托管原文件 | 100% | 高 |
| 深浅色、8 套主题、背景 | `localStorage` | 原代码不变 | 100% | 高 |
| 收藏、标签/图标排序 | `localStorage` | 原代码不变；favicon 代理加安全限制 | 100% | 高 |
| 头像 | 原 API 存本地文件 | 接入登录用户头像存储，保留原 API 兼容路径 | 100% | 高 |
| Excel 上传/删除/预览 | 服务端已可运行 | 迁入中心服务与持久存储 | 100% | 高 |
| 数据分析各面板 | Python 计算 + ECharts | 原 parser/metrics/API 迁移，底层改 SQLAlchemy | 100% | 高 |
| 异常修正 | SQLite | 迁 PostgreSQL，API 不变 | 100% | 高 |
| 截图列表/缩略图/看大图 | 遍历本地目录 | 查截图元数据表 + 服务端文件存储 | 100% | 高 |
| 截图删除 | 删本地文件 | 删元数据并回收存储文件 | 100% | 高 |
| 店铺管理 | YAML | 迁到数据库，再同步给 agent | 100% | 高 |
| 手动截图 | Patchright 本地执行 | 在线 API 建任务，agent 执行后上传 | 100% | 高，需 agent |
| 暂停/继续/进度 | 进程内变量 + JSON | 后端保存指令与进度，agent 轮询响应 | 100% | 高，需 agent |
| 淘宝登录 | 本机打开浏览器 | 网页下发登录指令，agent 打开本地浏览器 | 100% | 高，需 agent |
| 定时截图 | macOS `launchctl` | 时间配置存后端，agent 安装/更新本地定时任务 | 100% | 高，需 agent |
| 打开图片文件夹 | Finder/资源管理器 | 下发 agent 指令；agent 不在线时提示失败 | 100% | 中高，需 agent |
| 后台自启 | 单机主程序自启 | 改为本地采集 agent 自启 | 按钮与文案可不变 | 高，需 agent |
| 退出程序 | 关闭整个单机后端 | 应停 agent/当前任务并退出网站登录，不能关闭中心服务 | 外观可不变，系统语义必须调整 | 中高 |

## 4. “UI 和功能一模一样”的落地方法

### 4.1 前端不重写

不把现有页面重写成 React，不改 CSS 框架，不重做交互。直接复制：

- `frontend/shell/index.html`
- `frontend/screenshot/index.html`
- `frontend/analysis/index.html`
- 对应图标、ECharts 文件和其他静态资源

后端建立“兼容 API 层”，保留现有 `/api/screenshot/*` 和 `/api/analysis/*` 路径、请求字段与返回 JSON 形状。这样能把前端修改压缩到最小，同时最大限度避免视觉和功能回归。

### 4.2 唯一新页面：登录验证

参考协同版已有的登录机制，但本项目只保留一种普通账号，不显示角色、项目成员和管理员业务入口。

推荐流程：

1. 访问根路径。
2. `/api/auth/status` 检查会话。
3. 未登录时显示一个与现有蓝色管理风格一致的登录卡片。
4. 登录成功后进入原集合页，原页面不作二次包装。
5. 会话使用 HTTPS + Secure + HttpOnly + SameSite Cookie。

### 4.3 排除功能在集合页的处理

在线版不应出现点击后无法使用的空入口。推荐在构建在线版时，仅从 `PROGRAMS`、`APP_IDS`、对应标签与 iframe 中去掉“违禁词排查”和“主图一条龙服务”两个入口，其他 DOM、CSS、尺寸、图标排版不变。

如果“集合页也必须像素级保留四个图标”，则必须另外确定两个排除功能的真实去向：主图可连到已有协同版 URL；违禁词只能连接已安装的本地程序。不建议保留无功能占位图标。

## 5. 页面收集的推荐方案

### 5.1 为什么不能由网页 JavaScript 直接爬

普通浏览器页面受同源策略、跨域限制、登录 Cookie 隔离、截屏权限和后台运行限制影响，无法等价替代现有 Patchright 采集。即使使用浏览器扩展，也会引入安装、权限、版本兼容与应用商店分发问题，不如直接精简现有 Python 采集端稳定。

### 5.2 Agent 保留与删除的内容

Agent 保留：

- `screenshot/capture.py`
- `screenshot/login.py`
- OCR 工具
- 浏览器安装/定位、Patchright 依赖
- 本地 `browser_state/state.json`
- 本地定时任务安装、暂停/继续/停止控制
- 本地待上传队列和断网重试

Agent 删除：

- 整个集合页和分析页。
- 在线截图图库的最终元数据管理。
- 用户账号会话。
- 中心 Excel 与分析数据。

### 5.3 指令通道

不建议 HTTPS 网页直接 `fetch(http://127.0.0.1:...)`。这会遇到混合内容、浏览器 Private Network Access、CORS 与本地端口暴露问题。

推荐反向通信：

1. 网页请求后端创建任务。
2. Agent 使用单独设备 token 定时向后端获取指令，或建立一条出站 WebSocket。
3. Agent 执行 `capture_all`、`capture_shop`、`capture_url`、`pause`、`resume`、`login`、`open_folder`、`scheduler_install` 等指令。
4. Agent 上报心跳、当前店铺、进度、验证码和错误。
5. 截图成功后上传，后端确认校验值和元数据入库后，Agent 才从本地 outbox 标记为已完成。

这种结构不需要用户给网页开本地端口，对 HTTPS 、防火墙和路由器都更稳定。

### 5.4 上传策略

当前单图最大已超过 11 MB，因此：

- 不使用 base64 JSON。
- 使用 multipart 文件上传或对象存储直传。
- 单图上限建议 25–30 MB，反向代理限制至少 32 MB。
- 每张图携带 `capture_id + sha256 + brand + captured_at + original_name`。
- 后端对 `capture_id` 作唯一约束，保证重试不会产生重复图。
- 使用指数退避重试，本地保留待上传文件，直到服务端确认。
- 后端现场生成并缓存 480 px 缩略图，原图保留。

### 5.5 淘宝登录态边界

- 淘宝 Cookie/本地 storage state 只保存在 Agent 电脑。
- 不上传中心后端，不写入 PostgreSQL，不进入备份。
- 用户点“登录淘宝”时，后端只下发一个“打开本地登录窗口”指令。
- Agent 仅上报“已登录/未登录/流程进行中”，不上报账号秘密。

## 6. 服务端设计

### 6.1 建议数据表

| 表 | 作用 |
|---|---|
| `users` | 单一普通账号，可保留 `active` 和头像字段 |
| `user_sessions` | 哈希会话 token、过期时间 |
| `capture_agents` | Agent 设备标识、token 哈希、版本、平台、心跳、在线状态 |
| `screenshot_shops` | 店铺名、URL、启用状态和排序 |
| `capture_schedules` | 周几、时分、时区、启用状态 |
| `capture_jobs` | 任务类型、负载、状态、进度、错误、创建/开始/完成时间 |
| `screenshots` | 品牌、采集时间、文件名、storage key、大小、尺寸、SHA-256、Agent |
| `upload_batches` | 幂等上传批次和失败恢复 |
| `analysis_source_files` | 年份、原 Excel、storage key、SHA-256、上传时间 |
| `analysis_cache_files` | 按年份的解析缓存 storage key/版本 |
| `analysis_daily` | 现有日粒度核心指标 |
| `analysis_fix` | 现有人工修正记录 |

即使当前只有一个用户，业务表也建议保留 `user_id` 或单一 workspace 外键，成本很低，可避免将来添加第二个账号时重构数据。这不等于做多端协同锁。

### 6.2 API 兼容原则

原路径保持：

- `/api/analysis/upload`、`/status`、`/preview`、`/files`、`/metrics`、`/panel/*`
- `/api/screenshot/list`、`/file`、`/thumb`、`/delete`
- `/api/screenshot/shops`
- `/api/screenshot/capture`、`/capture/status`、`/capture/pause`、`/capture/resume`、`/progress`
- `/api/screenshot/scheduler/*`
- `/api/screenshot/login`、`/login/done`、`/login/status`
- `/api/screenshot/open-folder`
- 需要保留的 `/api/shell/*`、`/api/avatar/*`、`/api/health`

只在后端新增前端不直接调用的 Agent API，如：

- `POST /api/agent/pair`
- `POST /api/agent/heartbeat`
- `GET /api/agent/jobs/next`
- `POST /api/agent/jobs/{id}/start|progress|complete|fail`
- `GET /api/agent/config`
- `POST /api/agent/screenshots`

### 6.3 文件存储

两种部署方案都可用：

1. **中心电脑/局域网版**：与参照项目一样，PostgreSQL + 中心机本地不可变 storage 目录 + 定时完整备份。
2. **真正公网版**：PostgreSQL + COS/S3 私有对象存储 + HTTPS。Agent 可通过后端签名 URL 直传，避免长图经过应用进程和隧道。

如果继续复用参照项目的 Windows 中心机 + Cloudflare Tunnel，建议沿用其 COS 大文件中转经验，但本项目不需要复制协同会话的复杂业务模型。

## 7. 安全与运维底线

### 7.1 必须保留

- 密码使用强哈希（参照项目当前 PBKDF2 实现可复用）。
- 会话 token 只以哈希形式入库。
- 公网必须 HTTPS，Cookie 开启 Secure/HttpOnly/SameSite。
- Agent token 与网站密码分离，可以单独撤销。
- 检查上传文件大小、扩展名、魔数、图片解码和 SHA-256。
- 数据库和文件必须同时备份，并定期在临时环境验证恢复。
- 截图容量阈值与保留策略要在上线前定义。

### 7.2 需修补的线上风险

- 现有“收藏 favicon”由服务端请求任意 URL，在公网服务上会有 SSRF 风险。需禁止 localhost、私网 IP、非 HTTP(S) 协议，限制跳转、大小和超时。
- 所有变更型 Cookie API 应检查 Origin/Referer 或加 CSRF token。
- 登录接口需失败限速。
- Excel 解析、图片缩略图和大数据 JSON 返回应有超时、内存和并发限制。
- 不允许前端直接提供服务器文件路径；只使用不可猜测的 storage key/ID。

## 8. 数据迁移方案

### 8.1 不能只复制项目目录

现有数据分布在：

- `/Users/hanyang/.taobao_detail_extractor/db.sqlite3`
- `/Users/hanyang/.taobao_detail_extractor/analysis/cache.json`
- `/Users/hanyang/.taobao_detail_extractor/analysis/source/*.xlsx`
- `/Users/hanyang/策划实用小工具/screenshot/config.yaml`
- `/Users/hanyang/策划实用小工具/screenshot/screenshots/**`
- `/Users/hanyang/策划实用小工具/screenshot/browser_state/state.json`

其中 `browser_state/state.json` 不迁到后端，仅保留给 Agent。

### 8.2 一次性导入步骤

1. 对 SQLite、analysis 文件和 screenshot 目录做一份只读快照。
2. 导出两年 `analysis_daily`、`analysis_fix`。
3. 复制两个源 Excel 和按年缓存，计算 SHA-256。
4. 导入店铺与定时配置。
5. 逐张读取 27 张历史截图，从目录得出品牌/日期/时间，写入新存储和元数据表。
6. 在新系统重新计算相同 Excel 的面板 JSON，与旧 API 做结构和数值对比。
7. 校验截图数量、字节数、SHA-256、品牌/日期分组和缩略图可读性。
8. 旧程序改为只读或暂停写入，完成最后增量同步后切换。

### 8.3 浏览器本地设置

主题、背景、收藏、标签排序保存在旧 `127.0.0.1` origin 的 `localStorage`，新域名无法直接读取。这不影响业务数据，但可能使用户背景和收藏初次恢复为默认。

如必须保留这些个性化状态，可在旧程序加一个临时“导出设置”脚本，再由新站点首次登录导入；这是一次性迁移工具，不需要改正式 UI。

## 9. 分期实施计划

### 阶段 0：冻结基线（2–3 人日）

- 固定现有版本与数据样本。
- 记录两个迁移页在固定视口、深/浅色下的黄金截图。
- 为所有现有 API 保存请求/返回契约样本。
- 使用当前两年 Excel 保存全部分析面板期望结果。

### 阶段 1：中心服务骨架（3–4 人日）

- FastAPI、SQLAlchemy、Alembic、PostgreSQL。
- 登录页、单账号、Cookie 会话、退出。
- 静态前端托管与缓存策略。
- storage key、文件目录/对象存储接口。
- 健康检查、日志、基础备份。

### 阶段 2：首页数据分析在线化（4–6 人日）

- 原样搬迁 analysis 页。
- 迁移 parser、metrics 和所有 panel API。
- SQLite SQL 改 SQLAlchemy/PostgreSQL。
- Excel/缓存的持久存储与清理。
- 用 2025/2026 真实文件做新旧结果对比。

完成本阶段后，已可先交付一个“数据分析纯在线版”。

### 阶段 3：页面收集服务端（4–6 人日）

- 店铺、时间表、任务、Agent、截图和上传批次表。
- 截图列表/原图/缩略图/删除 API。
- 原 `/api/screenshot/*` 兼容层。
- 任务指令、心跳、进度和状态查询。
- 上传校验、幂等、断点重试与缩略图。

### 阶段 4：本地采集 Agent（6–9 人日）

- 从原程序拆出采集、登录、OCR 和调度。
- Agent 配对、token 安全存储、心跳和版本上报。
- 任务获取、暂停/继续/停止、登录窗口和本地目录。
- 本地 outbox、失败重试、校验后删除。
- macOS 打包和 `launchctl` 自启/定时任务。
- 如需 Windows 采集端，另加 2–4 人日做打包、任务计划程序和浏览器路径验证。

### 阶段 5：数据导入与切换（2–3 人日）

- 导入分析数据、Excel、缓存、店铺、时间表和截图。
- 保留本地淘宝登录态。
- 完成新旧数量、校验值和业务结果对账。
- 做一次回滚演练。

### 阶段 6：回归、部署与验收（4–6 人日）

- 视觉对比、API 契约、端到端测试。
- 真实淘宝登录、单店/全部店铺截图、裁剪和验证码流程。
- 断网、Agent 重启、后端重启、重复上传和大图测试。
- HTTPS、Cookie、Agent token、SSRF、文件上传与路径穿越安全测试。
- 备份与恢复演练。

### 工期汇总

- **仅首页数据分析在线可用**：约 9–13 人日。
- **macOS Agent + 页面收集 + 全部功能同等交付**：约 25–37 人日。
- **再加 Windows Agent**：约 27–41 人日。

上述是单人的稳妥工程估算，包含数据迁移、回归和部署，不只是写功能。若仅做“能跑的演示版”可更快，但无法满足“功能不变且长期可用”的目标。

## 10. 验收标准

### 10.1 UI 验收

- 固定 Chromium 版本与视口，对集合页、页面收集、数据分析做黄金截图对比。
- 浅色/深色、默认主题/自定义主题各至少一组。
- 对话框、浮动进度、时间选择、店铺管理、大图灯箱、分析各面板均纳入比对。
- 非登录页不允许出现字号、间距、颜色、图标、标签、按钮文案的主动改版。

### 10.2 数据分析验收

- 上传现有 2025/2026 Excel，年份、sheet 识别、异常数与预览一致。
- 各 panel API 的 key、数组顺序、日期范围与数值与旧系统一致。
- 日/月/季/半年/年/自定义时间范围、活动筛选、同比、新老客、板块、异常修正全部通过。
- 删除某年后，源文件、缓存、日数据和修正记录的清理语义与旧系统一致。

### 10.3 页面收集验收

- Agent 不在线时，网页能给出可理解的失败提示，不产生假任务。
- Agent 在线时，全部店铺、单店铺、任意 URL 三种触发均可用。
- 暂停、继续、停止、当前店铺、总进度与完成通知行为与旧系统一致。
- 淘宝登录、登录过期、验证码、OCR 裁剪与最大高度处理通过真实页面验证。
- 断网后截图保留本地，网络恢复自动上传，不丢图、不重复。
- 历史 27 张截图全部可按原品牌/日期浏览，原图、缩略图和删除正常。
- 定时任务在重启电脑后仍能触发，执行结果能上传。

### 10.4 运维验收

- 后端重启后未完成上传/任务状态可恢复。
- 数据库与文件备份可在空环境恢复。
- 达到容量告警阈值时可见，且不影响已有图片读取。
- 旧程序在回滚窗口期保持可恢复，切换不直接删除原数据。

## 11. 主要风险及应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 把页面收集误估为纯 Web | 核心功能无法落地 | 从一开始就采用 Agent 边界 |
| 长图增长过快 | 磁盘/对象存储费用增长 | 保留期、容量告警、原图/缩略图分层 |
| 网络中断 | 截图丢失或重复 | 本地 outbox + SHA-256 + capture_id 幂等 |
| 淘宝反爬/验证码 | 采集失败 | 保留现有登录、验证码检测、重试和错误图能力 |
| 前端看似不改但 API 字段改了 | 隐性交互回归 | 契约测试，保留原 URL/字段/状态码 |
| 旧系统没有自动测试 | 重构中难以发现回归 | 先建黄金截图和真实 Excel 基线，再迁移 |
| `localStorage` 不跨域名 | 主题/背景/收藏恢复默认 | 可选一次性设置导出/导入 |
| 中心服务被“退出程序”关闭 | 在线系统不可用 | 将语义限制为退出账号/停 Agent，绝不暴露服务器关机 API |

## 12. 推荐的最终技术选型

- 后端：Python 3.11+ / FastAPI。
- ORM 与迁移：SQLAlchemy 2 / Alembic。
- 数据库：PostgreSQL。
- 前端：直接复用现有静态 HTML/CSS/JS/ECharts，不重写。
- 认证：参考协同版的用户/会话实现，简化为单角色。
- 采集端：从现有 Python/Patchright 模块拆出轻量 Agent。
- 任务通道：Agent 主动轮询或出站 WebSocket，不让网页访问 localhost。
- 文件：局域网可用中心机 storage；公网优先 COS/S3。
- 部署：可复用参照项目现有 Windows 中心机/PostgreSQL/备份/HTTPS 经验。

## 13. 最终判断

该迁移不存在需要放弃的核心功能。在接受“页面收集需要本地 Agent”这一工程事实的前提下，可以做到：

- 除登录验证页外，两个迁移功能的 UI 不重设计。
- 用户操作路径、按钮、对话框、进度与结果展示保持不变。
- 分析数据和截图在后端集中保存、备份和访问。
- 淘宝登录态留在用户本地，不上传。
- 不引入没有必要的多端锁、成员角色和协同状态机。

**建议立项，并按“先分析、后采集”的顺序实施。** 数据分析是低风险的纯在线迁移，可先建立登录、数据库、文件存储和视觉回归基线；然后再接入需要本地 Agent 的页面收集，能显著降低整体切换风险。
