# Agent 重复副本与在线更新替换修复说明

## 一、问题现象

用户启动 Agent 后看到名称为 `PracticalToolsAgent (2)`，同时在线更新曾出现更新失败、更新后中心主机服务没有重新启动等问题。

当前项目构建配置中的标准 Agent 名称是 `PracticalToolsAgent`，代码中没有主动追加 `(2)` 的逻辑。因此，`(2)` 大概率来自 macOS 在复制、解压或更新时发现同名文件后自动生成的重复副本，也可能是启动器重复启动后的外部显示名称。

需要重点排查：

1. 在线更新是否把新 Agent 直接复制到旧 Agent 所在目录，导致 macOS 生成 `PracticalToolsAgent (1)`、`PracticalToolsAgent (2)` 等副本。
2. 更新清单是否允许把 Agent 放到错误的相对路径，或把不应直接覆盖的文件当成普通资源处理。
3. 启动器是否启动了更新包中的副本，而不是固定安装目录中的标准 Agent。
4. 更新失败后是否仍然保留了临时目录、锁文件或半成品文件。
5. Agent 更新成功后，中心主机服务是否一定会停止旧进程并重新启动新版本。
6. 页面收集相关资源是否因为更新包路径、缓存目录或版本切换而使用了旧代码。

## 二、标准路径和命名要求

以安装目录为唯一运行目录，不允许从下载目录、解压目录或临时目录直接运行 Agent。

标准可执行文件名必须保持为：

```text
PracticalToolsAgent
```

以下名称都应视为异常副本，不得作为正式运行目标：

```text
PracticalToolsAgent (1)
PracticalToolsAgent (2)
PracticalToolsAgent-copy
PracticalToolsAgent.tmp
```

启动前应记录并校验：

- 实际可执行文件绝对路径；
- 当前版本号；
- 文件是否可执行；
- 文件 SHA-256；
- Agent 使用的数据目录；
- Agent 连接的中心主机地址和端口。

如果实际运行路径不是安装目录下的标准 `PracticalToolsAgent`，应停止启动并给出明确提示，不要静默运行副本。

## 三、在线更新的安全替换流程

不要直接把新文件复制到正在运行的 Agent 文件上，也不要使用可能产生 Finder 重名副本的普通复制流程。

建议统一采用以下流程：

```text
下载更新包
  ↓
校验清单格式和路径
  ↓
校验文件 SHA-256、大小和版本
  ↓
解压到独立临时目录
  ↓
验证临时目录中的标准 Agent 文件
  ↓
停止中心主机和 Agent 旧进程
  ↓
将旧安装目录改名为 .backup-版本号-时间戳
  ↓
将临时目录原子改名为正式安装目录
  ↓
恢复必要配置和数据目录
  ↓
启动新版本中心主机/Agent
  ↓
健康检查和版本确认
  ↓
成功后删除旧备份；失败则回滚
```

实现要求：

1. 更新包必须先落到临时目录，不能边下载边覆盖正式目录。
2. 清单中的路径必须是相对路径，禁止绝对路径、`..` 路径和路径穿越。
3. 清单中如果包含 Agent，必须限制在允许的文件名和目录内，例如安装根目录下的 `PracticalToolsAgent`。
4. 解压后必须检查是否出现 `PracticalToolsAgent (2)` 等重复名称；发现后直接判定更新包无效。
5. 替换过程必须具备回滚能力，不能先删除旧版本再发现新版本不可运行。
6. 更新锁必须使用唯一锁文件，并在异常退出时可靠清理；启动时要清理过期锁，但不能删除仍被活跃进程持有的锁。
7. 所有步骤都要写入更新日志，至少记录：下载、校验、停止进程、替换、启动、健康检查和回滚结果。

## 四、进程停止和重启要求

更新程序只能终止属于本项目的进程，禁止使用宽泛的命令终止所有 Python、所有浏览器或所有同名系统 Agent。

终止前应通过以下信息确认目标：

- 可执行文件绝对路径；
- 启动参数中是否包含本项目安装目录；
- 父进程和子进程关系；
- PID 是否仍然存活。

推荐顺序：

1. 向中心主机发送优雅退出信号。
2. 等待有限时间，并轮询进程是否退出。
3. 仍未退出时，只对已确认属于本项目的 PID 发送强制终止信号。
4. 重新读取进程列表，确认旧进程已经退出。
5. 启动新版本。
6. 通过健康检查接口确认服务可用，并确认返回的版本号正确。

更新程序不得只执行“停止旧进程”而不确认“新进程已成功启动”。如果启动失败，必须自动回滚到备份版本并恢复服务。

## 五、启动器修复要求

启动器需要固定解析安装目录，不要根据用户最近打开的文件、下载目录或 Finder 选中的文件决定启动目标。

启动前应：

1. 获取安装目录的标准 Agent 路径。
2. 检查标准文件存在且可执行。
3. 检查同目录是否存在带序号的重复副本。
4. 如果发现重复副本，优先记录告警；不要自动猜测应该运行哪一个。
5. 检查是否已有同一安装目录的 Agent 在运行，避免重复启动。
6. 启动后读取健康检查结果，确认进程路径、版本号和连接状态。

如果必须兼容旧版本目录，应先将旧目录迁移到明确的备份目录，再创建干净的正式目录，不能在原目录中混合覆盖。

## 六、更新包清单修复要求

更新清单校验需要同时防止路径错误和版本错配：

- 禁止绝对路径；
- 禁止 `..` 路径；
- 禁止以 `/` 开头的路径；
- 禁止包含控制字符；
- 统一使用 `/` 作为清单分隔符；
- 文件名必须与允许列表匹配；
- 每个文件必须有大小和 SHA-256；
- 清单版本必须与更新包版本一致；
- 清单不能把源码目录中的开发文件直接当成可执行更新文件；
- `agent/agent.py` 这类源码路径不能在只允许打包产物的更新清单中直接出现，除非更新机制明确支持源码包并且目标路径经过映射。

建议把“源码更新包”和“可执行在线更新包”分成两种格式，不要混用：

```text
source-package/
  agent/agent.py

runtime-update-package/
  PracticalToolsAgent
  center-host/
  manifest.json
```

## 七、页面收集相关更新必须确认已打包

页面收集本轮已经涉及以下功能，主进程需要逐项确认源码、构建产物和更新清单都包含最新版本：

1. 图片缩略图只展示原图顶部区域，不是把整张原图压缩后居中展示。
2. 缩略图使用固定裁剪区域和顶部对齐，例如 `object-fit: cover` 配合 `object-position: top`，不能因为容器比例改变而退化成整图缩略。
3. 缩略图不得在筛选品牌时重复下载同一资源。
4. 缩略图应使用稳定缓存键，缓存键至少包含图片 URL、尺寸/裁剪参数和资源版本。
5. 筛选品牌时优先复用内存缓存和本地缓存，只加载新增且确实缺失的图片。
6. 缩略图加载失败时不能进入无限重载循环，应有失败状态、重试次数上限和退避时间。
7. 图片组件卸载或筛选切换时要取消旧请求，避免旧请求完成后反复触发状态更新。
8. 打开图片后自动预加载前后各 2 张原图。
9. 预加载请求要有并发上限、去重和取消机制，不能阻塞当前图片显示。
10. 原图缓存需要设置容量/数量上限，避免持续占满磁盘。
11. 程序退出时清理本次会话产生的原图预加载缓存；用户明确保存的文件不得误删。
12. 切换上下张时优先使用已完成的本地缓存，未完成时显示轻量加载状态，不重复创建请求。

## 八、建议排查的代码位置

优先检查以下目录和文件：

```text
agent/agent.py
agent/scheduler.py
packaging/agent_mac.spec
packaging/build_mac.sh
packaging/install_agent.command
packaging/build_release.py
```

同时搜索以下关键词，确认是否存在重复启动、直接覆盖、无限重试或路径拼接问题：

```text
PracticalToolsAgent
subprocess
Popen
os.replace
shutil.copy
shutil.copy2
kill
terminate
restart
manifest
update
retry
cache
prefetch
object-position
IntersectionObserver
```

## 九、验收测试

### 1. Agent 名称和路径

- 全新目录安装后只有一个标准 `PracticalToolsAgent`。
- 连续执行 3 次更新，不产生 `(1)`、`(2)` 或 `-copy` 文件。
- 从启动日志中可以看到实际运行绝对路径。
- 重启程序后仍然使用同一个标准安装目录。

### 2. 在线更新

- 更新包下载中断时，旧版本仍可运行。
- 清单校验失败时，不停止正在运行的服务，或停止后能自动恢复。
- 更新成功后旧进程确实退出，新进程成功启动。
- 新进程健康检查通过，版本号与清单一致。
- 新进程启动失败时能自动回滚，中心主机服务恢复。
- 更新后不需要用户手动重复启动中心主机服务。

### 3. 页面收集

- 缩略图展示图片顶部区域，视觉效果与用户提供的示例一致。
- 首次进入页面不会一次性加载所有原图。
- 品牌筛选来回切换时，已经缓存的缩略图不重复请求。
- 快速上下翻图时，前后两张原图能够被预加载并复用。
- 连续打开和关闭预览窗口 20 次，不出现疯狂重载、空白或请求数量持续增长。
- 网络失败后不会无限重试。
- 退出程序后，本次会话的预加载缓存被清理。

### 4. 日志

日志需要能区分以下事件：

```text
UPDATE_START
MANIFEST_VALIDATED
PACKAGE_VERIFIED
OLD_PROCESS_STOPPED
INSTALL_SWITCHED
NEW_PROCESS_STARTED
HEALTH_CHECK_OK
ROLLBACK_STARTED
ROLLBACK_OK
THUMBNAIL_CACHE_HIT
THUMBNAIL_CACHE_MISS
ORIGINAL_PREFETCH_START
ORIGINAL_PREFETCH_DONE
```

## 十、最终交付要求

完成修改后重新构建并交付 `0.4.0` 完整包，同时保留可用于在线更新的清单和文件哈希。

交付前必须确认：

1. 完整包内没有重复 Agent 文件名。
2. 在线更新清单中的路径都能被当前更新器接受。
3. 完整包和在线更新包的版本号一致。
4. macOS 启动脚本能从标准安装目录启动 Agent。
5. 更新失败可以回滚并恢复中心主机服务。
6. 页面收集的顶部裁剪、缩略图缓存、原图前后 2 张预加载和退出清理都已进入最终构建产物。
7. 打包后对最终产物做一次文件列表、哈希、启动和健康检查，不只检查源码。

